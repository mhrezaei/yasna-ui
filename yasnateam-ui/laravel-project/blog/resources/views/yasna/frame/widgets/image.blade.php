<img
        class="{{ $class or "" }}"
        id="{{ $id or "" }}"
        src="{{ $src or "" }}"
        alt="{{ $alt or "" }}"
        width="{{ $width or "" }}"
        height="{{ $height or "" }}"
>